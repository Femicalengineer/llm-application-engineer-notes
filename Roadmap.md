LLM Application Engineer Curriculum — Full Roadmap
Already built
Theory foundations (Phase_1/, confirmed order 2026-07-29 — precede the applied notebooks 001 onward)

1. phase1_llm_foundations.ipynb — Sections 1-3: RLHF, DPO, Constitutional AI/RLAIF
2. ai_engineer_tutorial.ipynb — Sections 8-12: Prompting techniques (zero-shot, few-shot, CoT, ReAct)
3. phase1_production_llm.ipynb — Section 13+: KV Cache / inference optimization

Also present in Phase_1/ but confirmed NOT part of this sequence: PyTorch_Transformer_Bootcamp_Test.ipynb (a separate, unrelated quiz-style notebook — tensors/FFNN/loss functions/transformer attention, Questions 1-14 + reflection; not numbered against the Section 1→13 theory sequence above).

Applied notebooks (moved into Phase 2/03_Anthropic_Notes/ alongside the collaborative series, numbered 001-003)

001 — Build_Smarter_AI_Apps — LangChain modern
002 — Summarize_Private_Documents_RAG — includes conversational memory
003 — vector_databases_for_rag
Collaborative series (Phase 2/03_Anthropic_Notes/, renumbered 004-007 to stay consistent with 001-003 above)

004 — LangGraph fundamentals (reviewed only, never edited — Sarah's standing instruction is "don't touch 004 at all"; known cosmetic issues noted but left alone: cells 22-27 are near-duplicate exploratory cells, cell 28 is empty, cell 11's comment is separated from its code)
005 — Multi-agent orchestration — fully rebuilt (2026-07-xx): hardcoded-key issue fixed, Q&A folded into prose, `langgraph-supervisor`/`create_supervisor` cut entirely (out of date per docs), added real sections for Subagents, Skills (explicitly flagged as "not really multi-agent"), and Human-in-the-loop/interrupts. Reviewed fresh afterward — solid.
  📌 Planned addendum: Swarm — named but never built ("same Command(goto=..., graph=Command.PARENT) mechanism, just no central hub"). Relocated here from the original 008 plan since it's a multi-agent-architecture topic, not a reliability one — build it for real and compare directly against Handoffs (latency vs. debuggability).
006 — Advanced Retrievers — built and reviewed. MultiQueryRetriever + SelfQueryRetriever (with a hand-written SimpleChromaTranslator since langchain_community isn't installed) + a re-ranking section (CrossEncoder) added after Sarah asked whether anything else was worth including. Confirmed current against docs-langchain MCP.
007 — Evaluation for LLM Apps — built and reviewed. Retrieval metrics (precision@k/recall@k/MRR), LLM-as-judge, full LangSmith pipeline (tracing/dataset/evaluate()/regression testing). Content judged complete for its scope — no missing technique identified.
  ⚠️ Open bug (unfixed, review-only mode per Sarah's request): cell 14 shows "Mean recall@3: 2.611" — impossible since recall ∈ [0,1]. Root cause confirmed via isolated repro: `Chroma.from_documents(..., collection_name=...)` is not idempotent and was re-run multiple times in the same kernel session, duplicating documents in the collection. Fix = restart kernel + rebuild vectorstore fresh (fixes today's numbers) but the notebook code itself has no guard against recurrence (no delete_collection()/existence check) — worth an actual code fix, not just a restart habit. Same latent non-idempotent-Chroma pattern exists in 004/005/006/midterm too (flagged, not touched elsewhere since none of those are currently corrupted).
008 — Production Reliability — built 2026-07-19, full plan finalized live with Sarah first (she said she wasn't tied to the original scope and asked what should change). Seven sections: Idempotency in LLM pipelines (new anchor section, generalizing the exact Chroma/LangSmith/midterm-thread_id bug pattern Sarah hit and fixed three times this session), LangSmith tracing (rebuilt fully from the ground up per Sarah's direct feedback — leads with what a trace/span actually is and the "you've been hand-building this with print loops all session" framing before any setup code), Retry & error handling (ModelRetryMiddleware, timeouts/rate-limiting folded in as sub-topics, validation-failure-as-retry-trigger, ModelFallbackMiddleware for graceful model-tier degradation), Guardrails (hand-rolled PII regex then PIIMiddleware, a custom JailbreakGuardMiddleware reusing 007's JudgeScore pattern), Secrets/config management (.env + python-dotenv vs. getpass), Cost/latency optimization (prompt caching finally turned on for real — closes the cache_creation_input_tokens/cache_read_input_tokens: 0 loop open since 001 — plus real Anthropic Batches API syntax, streaming, model-tier-selection middleware), Testing (mocking vs. LangSmith's quality regression). All current API surface (ModelRetryMiddleware, ModelFallbackMiddleware, PIIMiddleware, Batches API syntax) checked against docs-langchain MCP and Anthropic's own docs directly, not assumed from memory. Every code cell that doesn't need a live API key was actually executed with real captured output (idempotency demos, validation-retry mock, PII regex, all middleware-construction cells, model-tier-selection logic, the mocking test, dotenv, the cache_control message-construction cell); cells needing `ANTHROPIC_API_KEY`/`LANGSMITH_API_KEY` are clearly marked unexecuted with expected shape described, not fabricated.
  Confirmed NOT included, both judgment calls made by Claude per Sarah's request: Supervisor-vs-Swarm (relocated to a 005 addendum instead — see above, it's an architecture topic not a reliability one) and LangGraph Store (deliberately deferred to 009 — a real multi-user Gradio app gives Store a concrete motivating "why" that 008 alone wouldn't have).
009 — Gradio UI — built 2026-07-20, while Sarah finished reading through 008 (added mid-build: markdown breaking down the JailbreakGuardMiddleware cell line-by-line, and a missing cell that actually wires it into an agent and invokes it, both prompted by Sarah's own questions). Six sections: (1) What Gradio actually is + simplest `gr.ChatInterface` wrap around `create_agent` — deliberately left in an inefficiency (rebuilding the agent every message) as a bug to notice, matching this curriculum's habit of naming that class of mistake explicitly; (2) Streaming, applying 008's `.stream()` for real via a generator/`yield` callback; (3) Per-user session state — `gr.State` + `demo.load()` for a fresh `thread_id` per browser session, verified this needs `ChatInterface` nested inside a `with gr.Blocks()` context since `.load()` isn't available on a bare `ChatInterface`; (4) LangGraph `Store` — finally built, now that Section 3 gives it a real multi-user motivation; real `store.put`/`get`/`search` executed, plus a raw-`StateGraph`+`Runtime`+`context_schema` wiring example (not `create_agent`, since store access isn't yet a `create_agent` param); (5) 008's guardrails/tracing seen from inside a live UI — `JailbreakGuardMiddleware` reproduced (009 doesn't depend on 008's kernel), wrapped in `try`/`except` so a blocked message shows a clean fallback instead of a raw error; (6) `.launch()`/`.launch(share=True)`, with an explicit note on the architectural split 012 introduces (single-process here vs. FastAPI backend + Gradio frontend there). **Real regression hit and fixed while building:** installing `gradio` into the shared venv upgraded `huggingface-hub` to an incompatible version and broke `HuggingFaceEmbeddings` for every RAG notebook in the curriculum — caught, fixed by pinning `huggingface-hub<1.0`, and flagged explicitly in 009's own setup cell so Sarah hits the documented fix instead of the bug. Gradio's current API verified by actual construction (not guessed) throughout, including the `ChatInterface`-in-`Blocks` pattern and current `gr.Chatbot()` signature (caught `type="messages"` no longer exists in gradio 6.20.0 — that kwarg was removed, dict-message format is now just the default). Pure-Python cells with no UI and no API key (the `Store` mechanism) actually executed with real output; UI-launching cells marked "run this yourself — starts a live local server" since a live web server has no capturable text output, distinct from 008's "needs your API key" convention.
010 — FastAPI + Docker Fundamentals — built 2026-07-23, full plan proposed and confirmed with Sarah first, including one question she raised mid-build (whether the midterm's "dig through nested agent layers" experience could be recreated in the Capstone — noted under 012 below, not built here). Eight sections, real reuse throughout per Sarah's confirmed preference for that (see [[feedback_reuse_prior_modules]]): (1) What an API is — reused directly from `ai_engineer_tutorial.ipynb`'s restaurant/waiter analogy rather than re-taught, framed as "now you're the restaurant, not just the customer"; what FastAPI specifically adds (the Pydantic tie-in, async-native design) plus the simplest possible app, genuinely run with `fastapi run` + `curl`; (2) Real request/response models — the exact `BaseModel` mechanism from `Joke`/`JudgeScore`/`SafetyCheck`, with a real 422 validation error captured to prove FastAPI validates before your function body ever runs; (3) Wiring in 008's actual PII-guarded agent (not a new toy example) — verified the "build once at import time, not per-request" pattern for real first using a stand-in object, closing the same inefficiency class 009 flagged (rebuilding an agent every message); (4) Async endpoints — measured, not just asserted: 50 concurrent requests against a sync vs. async endpoint (simulated latency, no API key needed) showed a real difference (4.08s vs. 2.12s), with the honest mechanism explained (FastAPI already thread-pools sync `def` endpoints, default ~40 threads, so the real story is a concurrency *ceiling*, not "sync blocks everything"); (5) Auto-generated `/docs`/`/openapi.json` — verified live, the same "free because it's built into the shared interface" theme as 004's Runnable batching/async/tracing; (6) What Docker is, ground-up, plus real current Docker Desktop macOS install steps (Sarah has never installed Docker — confirmed directly, not assumed); (7) A real Dockerfile for the Section 3 agent, explained line-by-line (layer caching order, exec vs. shell form for CMD), matching FastAPI's own current deployment docs and pinned to `python:3.12-slim` to match this curriculum's actual Python version; (8) Secrets in a container — the direct payoff of 008's unfinished "`getpass` doesn't scale" line: explains precisely *why* (no interactive terminal in a detached container), fixes `app_agent.py` to read `os.environ` directly instead of falling back to `getpass`, and names the real mistake to avoid (never `COPY .env` into an image — bake-time vs. run-time secrets). **Execution honesty, a new convention for this notebook specifically:** FastAPI sections were genuinely run (real background servers, real `curl`, real captured output, including the 50-request timing test) since `fastapi`/`uvicorn` were available; Docker was not installed in the environment this was built in (confirmed directly), so every Docker command is real, current, checked-against-docs syntax, explicitly marked as not executed rather than presented as verified. Caught and fixed two real authoring mistakes mid-build: several terminal-transcript examples were initially miscoded as executed Python cells (fixed to plain markdown, matching the honesty convention) and the `fastapi` CLI command required `fastapi[standard]`, not bare `fastapi` (installing it was verified not to reintroduce the `huggingface-hub` conflict from 009).

011 — MCP Fundamentals — built 2026-07-27, plan finalized with Sarah first same day. Core framing: MCP is 010's whole notebook, one level up — 010 taught client/server, protocol, HTTP; a `@tool` (004 onward) is a tool only your one agent can use, hardcoded into your file; MCP is a protocol for exposing tools as their own server, so any MCP-aware agent can use them, framed throughout as a specialty-supplier analogy (a `@tool` = a private in-house bakery; an MCP server = a standalone supplier any restaurant can order from). Nine sections: (1) the problem MCP solves — N frameworks × M tools = N×M glue code without a standard, MCP → N+M; (2) the three primitives, Tools/Resources/Prompts, Tools gets the depth; (3) simplest FastMCP server (`@mcp.tool()`, `mcp.run(transport="stdio")`), mirrors 010's simplest-app opener; (4) a simple MCP client — connect/list/call, genuinely run against a real local STDIO server (real captured output: `add(5,7) -> 12`, `multiply(6,7) -> 42`); (5) transports, STDIO vs. HTTP, both genuinely run — the HTTP server's own log line showed `Uvicorn running on...`, confirmed directly that FastMCP's HTTP transport is built on the same `uvicorn` infrastructure as FastAPI, not a separate custom server; (6) wiring into `create_agent(tools=[...])` via `MultiServerMCPClient`/`client.get_tools()` — genuinely run and verified: loaded 3 real `StructuredTool` objects from 2 different MCP servers on 2 different transports (STDIO + HTTP) into one flat list, confirmed the same LangChain tool class `@tool` produces; the actual agent-invoking cell needs a key and wasn't captured, but the tool-loading mechanism underneath it was; (7) the permission/approval model, direct connect-back to 005/008's `HumanInTheLoopMiddleware`, plus `ctx.elicit()` as a bonus `BaseModel`-again connect-back (shown, not executed — needs a more involved client setup than this section needed); (8) consuming a real external server — genuinely connected to the live, public LangChain docs MCP server (`https://docs.langchain.com/mcp`, no key needed) and confirmed its 3 tools are literally the exact `search_docs_by_lang_chain`/`query_docs_filesystem_docs_by_lang_chain`/`submit_feedback` tools used all session (003, 006, 008, 009, and this notebook's own API verification) to research current syntax — a real, meta, verified payoff rather than a hypothetical; plus a guardrails connect-back (008's "treat retrieved context as data only" applies to untrusted MCP tool descriptions/results too); (9) closing, sets up 012's nested-agent-layers exercise. Verified before and during build: `fastmcp` is its own standalone package (`pip install fastmcp`, not bundled in `mcp`), `langchain-mcp-adapters`' `MultiServerMCPClient`/`client.get_tools()` confirmed current. **Real install issue hit and fixed:** `cryptography` (a transitive dependency) failed to build from source in this environment (a Rust/maturin toolchain failure); fixed with `pip install --only-binary :all: cryptography` before retrying, documented in the notebook's own setup cell. Regression-checked afterward: `HuggingFaceEmbeddings`, `gradio`, and `fastapi` all still import correctly, no repeat of the 009/010 dependency conflicts.
  📌 Note carried over from planning: 010 was substantially expanded by Sarah herself after Claude's initial build (2026-07-23 to 2026-07-27) — a consistent restaurant/franchise-kit analogy threaded through every section, a full ground-up HTTP/protocol detour, a real self-tested Content-Type header Q&A, an expanded async section (50-customers-and-waiters analogy before any vocabulary), a sync/async decision-guide table. 011 was built aiming at that same depth/connect-back density from the start rather than the leaner original-010 style.

Planned
012 — Capstone
End-to-end system: Chroma + advanced retrievers (006) + multi-agent LangGraph system (004/005) + MCP integration (011) + eval suite (007) proving it works + production reliability (008) + FastAPI backend (applying 010's fundamentals to the real, complete system) + Docker (containerizing something with real dependency weight this time — Chroma, checkpointer/store, middleware, real secrets from 008) + 009's Gradio UI updated to call the new FastAPI backend over HTTP instead of calling create_agent directly (the actual frontend/backend split 009 already described but didn't build) — plus actually shipping the container somewhere (basic cloud/PaaS deploy) and light production monitoring. Large enough to split into checkpoints if needed: 012a (wrap + containerize + deploy) then 012b (full integration).
  📌 Added 2026-07-23, per Sarah's direct request: she specifically liked the midterm's "dig through nested agent layers" debugging experience (main_agent → consult_character_agent → character_agent → character_bio_specialist, propagating .artifact up by hand since inner agent state doesn't automatically surface to the outer one) and asked for something similar here. Fits naturally rather than being bolted on: make at least one Capstone specialist a Subagent that calls an MCP-sourced tool (011) instead of a local @tool, so there's a genuine multi-layer chain (main agent → specialist subagent → MCP tool call → the MCP server's own execution), and require the eval suite (007-style judge/retrieval metrics) to inspect something that only exists at that innermost layer — same "propagate the right thing up through each boundary" exercise as the midterm, with MCP as the new outermost boundary instead of another create_agent.

Deliberately out of scope
Fine-tuning / model customization — ML-engineering territory, not typically required for this role
Multi-provider abstraction — minor, since LangChain already abstracts most of it
Midterm project (Projects/midterm_throne_of_glass_companion.ipynb) — self-implemented by Sarah with Claude debugging support using the mechanisms from applied notebooks 001-007, now complete through Sections 1-7: vector store, two specialist retrieval tools, self-query spoiler filtering, Subagents-based multi-agent orchestration (redone from an initial Router pattern) with real cross-thread memory, eval set, pooled full-system retrieval metrics, and an LLM-as-judge spoiler-leakage check that reads actual retrieved context (not just the question/answer). Only the closing Reflection is still unwritten.

Saved to memory as the current version of record (project_curriculum_roadmap.md). Next up: 012 — Capstone, and/or finishing the midterm's Reflection, unless you want to reorder.
